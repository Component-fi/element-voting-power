type VoteChangeEvent @entity {
  id: ID!
  timestamp: BigInt!
  from: Voter!
  to: Voter!
  amount: BigInt!
}

type Voter @entity {
  id: ID!
  FromEvents: [VoteChangeEvent!]! @derivedFrom(field: "from")
  ToEvents: [VoteChangeEvent!]! @derivedFrom(field: "to")
  votingPower: BigInt!
}