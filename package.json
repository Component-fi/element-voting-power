{
  "name": "element-voting",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ element-voting",
    "create-local": "graph create --node http://localhost:8020/ element-voting",
    "remove-local": "graph remove --node http://localhost:8020/ element-voting",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 element-voting"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.25.1",
    "@graphprotocol/graph-ts": "0.24.1"
  }
}
